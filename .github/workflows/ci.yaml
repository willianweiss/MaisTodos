name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build, test, lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Build the app
      run: make build
    - name: Test the app
      run: make test
    - name: Lint the code
      run: make lint
